<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (http/ws) exception scoped to the local dev backend only —
  10.0.2.2 (the Android emulator's alias for the host machine) and
  localhost. NEVER `cleartextTrafficPermitted="true"` app-wide (design
  §10): a release build pointed at a real deployment must use
  https/wss (via the API_BASE_URL dart-define), which this config does
  not weaken since it names only these two dev hosts.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
