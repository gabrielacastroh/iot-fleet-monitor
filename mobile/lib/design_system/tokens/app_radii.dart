/// Literal port of the CSS `calc()` radius scale in
/// `frontend/src/index.css` (`--radius: 1rem`). See design §2.4.
class AppRadii {
  const AppRadii._();

  static const sm = 6.0;
  static const md = 8.0;
  static const lg = 12.0;
  static const xl = 16.0;
  static const xxl = 20.0;
}
